import {
  StyleSheet
} from 'react-native';

const buttonStyle=StyleSheet.create({
    header: {
      marginTop:100,
      flex:1
    },
    text: {
      fontSize:32
    }
  })

  export default buttonStyle;